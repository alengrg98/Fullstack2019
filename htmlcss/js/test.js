var x=10;           //var creates global variables
if(x<15)
{
    let x=20;               //let creates a local variable and const is used to create constants
    x=x+1;
    console.log(x);
}
console.log(x);

